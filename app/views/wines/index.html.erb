<h1>Wines</h1>
<p>Find me in app/views/wines_controller/index.html.erb</p>
<ol>
<% @wines.each do |wine| %>
   <li> <%= link_to(wine.name, wine) %> by  <%= wine.year%> </li>
   <% end %>
</ol>
  <%# by  <%= wine.year -%> 
  <%# by  <%= wine.winery -%> 
<%= link_to 'New Wine', new_wine_path %>
